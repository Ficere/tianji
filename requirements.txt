# 天机 / Tianji —— Python 依赖
#
# 安装：pip install -r requirements.txt
#
# 说明：脚本在依赖缺失时不会崩溃，而是走降级路径并在输出 JSON 的
# `warnings` 字段中标记。但降级结果不应作为正式解读依据。

# —— 必需 —— #
# 公历→农历转换。缺失时农历退化为「正月初一」，称骨结果不可用。
zhdate>=0.1

# —— 强烈建议（境外出生地必需）—— #
# 经纬度→IANA 时区名离线查询。缺失时时区按经度粗略估算，
# 遇夏令时/历史时区变更会导致上升星座偏差约 1 小时（约半个星座）。
timezonefinder>=6.0
pytz>=2023.3

# —— 可选 —— #
# 更高精度的月亮星座计算。缺失时使用内置 Meeus Ch.47 近似算法（±1°）。
# ephem>=4.1
